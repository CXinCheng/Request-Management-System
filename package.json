{
    "name": "root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "concurrently \"cd client && npm install\" \"cd server/auth && npm install\"",
        "start": "concurrently \"npm run dev --prefix client\" \"npm run start --prefix server/auth\" \"npm run start --prefix server/request-service\""
    },
    "devDependencies": {
        "concurrently": "^7.0.0"
    },
    "dependencies": {
        "chart.js": "^4.4.6",
        "vue-chartjs": "^5.3.2"
    }
}
